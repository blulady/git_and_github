>git init #in directory I want the git repository I am in, after making my .gitignore file
>git add --all
>git commit -m "initial commit" 
>git branch -M main #or if you want to push master instead of main leave this command out
>git remote add orgin https://github.com/blulady/moar_django.git
>git push -u origin main #>git push -u origin master

*If I want to use master instead of main
>git init #in directory I want the git repository I am in, after making my .gitignore file
>git add --all
>git commit -m "initial commit" 

>git remote add orgin https://github.com/blulady/moar_django.git
>git push -u origin master